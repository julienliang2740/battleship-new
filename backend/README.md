# Battleship Backend

Express + TypeScript REST API. Run `npm install && npm run dev` and it listens on
port `4000`.

See `../design_docs/04-api-contracts.md` for the API contract and
`../design_docs/05-backend-modules.md` for the file layout.
